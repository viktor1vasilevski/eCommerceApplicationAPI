namespace Main.DTOs.Category;

public class CreateCategoryDTO
{
    // Here we can add some property to the class. For example we can return the Id and the Name (check CreateProductDTO).
    // It depends if we need to return the new object. In this case I decited to read just the success of the api response
    // and if this is true it means that the create was successful.
}
